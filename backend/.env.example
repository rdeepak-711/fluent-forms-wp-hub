# --- App Details ---
PROJECT_NAME="Fluent Forms Hub"
VERSION="0.1.0"
API_V1_STR="/api/v1"
DEBUG=True

# --- Security ---
# Generate with: openssl rand -hex 32
SECRET_KEY="changethis_secret_key_generated_by_openssl"
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY="generate_a_real_key_using_the_command_above"
ALGORITHM="HS256"

# --- Database (MySQL) ---
# Format: mysql+pymysql://user:password@host:port/db_name
# For local dev without docker, usually localhost
# DATABASE_URL is derived automatically from DB_USER, DB_PASSWORD, DB_HOST, DB_PORT, DB_NAME

# --- Redis (Cache & Celery) ---
# Format: redis://host:port/db_number
REDIS_URL="redis://localhost:6379/0"

# --- Email (SMTP) ---
SMTP_TLS=True
SMTP_PORT=587
SMTP_HOST="smtp.gmail.com"
SMTP_USER="drive.deepak1@gmail.com"
SMTP_PASSWORD="app_password_here"
EMAILS_FROM_EMAIL="drive.deepak1@gmail.com"
EMAILS_FROM_NAME="Fluent Forms Hub"

# --- WordPress (Spoke Configuration Defaults) ---
# These might not be global but useful for default testing
# Individual sites will have their own keys in the database, 
# but this could be for a master key or initial sync testing.
WP_DEFAULT_API_KEY="optional_default_key"
WP_DEFAULT_API_SECRET="optional_default_secret"

# ---- Crypt ALGORITM ----
CRYPT_ALGORITHM="HS256"

# ---- CORS ORIGINS -----
CORS_ORIGINS=""